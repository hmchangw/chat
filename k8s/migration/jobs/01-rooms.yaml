# Phase 1 — rooms
# Source:  rocketchat.rocketchat_room + rocketchat.rocketchat_subscription (for metadata)
# Target:  chat.rooms
# Depends: nothing (reads source only)
apiVersion: batch/v1
kind: Job
metadata:
  name: migrate-rooms
  namespace: migration
spec:
  backoffLimit: 10
  activeDeadlineSeconds: 3600   # ~1M rooms takes ~3-4 min
  template:
    spec:
      restartPolicy: OnFailure
      containers:
      - name: migrate-rooms
        image: ${MIGRATION_IMAGE}
        command: ["/migrate-rooms"]
        envFrom:
        - configMapRef:
            name: migration-config
        env:
        - name: SOURCE_MONGO_URI
          valueFrom:
            secretKeyRef:
              name: migration-vault-secret   # REPLACE with the name your VaultSecret creates
              key: SOURCE_MONGO_URI
        - name: TARGET_MONGO_URI
          valueFrom:
            secretKeyRef:
              name: migration-vault-secret
              key: TARGET_MONGO_URI
        resources:
          requests:
            cpu: "250m"
            memory: "256Mi"
          limits:
            cpu: "1"
            memory: "512Mi"

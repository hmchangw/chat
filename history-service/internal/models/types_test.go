package models

// UDT type tests (Participant, File, Card, CardAction JSON round-trips,
// Marshal/Unmarshal, verifyUDTTags) have moved to pkg/model/model_test.go
// since the types now live in pkg/model.

# Generated by docker-local/e2e/setup-e2e.sh; never commit.
secrets/

# Generated by e2e/harness.CaptureLogs on test failure; never commit.
# Note: the test process's working directory at test time is the package
# being tested (e2e/), not docker-local/e2e/. The corresponding logs/
# directory at the repo's e2e/logs path is ignored by this rule because
# it lives under a sibling tree; the root-level .gitignore covers it.


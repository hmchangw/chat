name: chat-e2e

# Two-site backend e2e topology. See docs/superpowers/plans/2026-05-08-e2e-tests.md
# and the companion amendments doc 2026-05-08-e2e-tests-amendments.md (R1-R4).
#
# Host port allocation (per amendment R2.A, with Mongo revised in R4.B to stay
# below the Linux ephemeral range 32768+):
#
#   Service              Internal  Site-A host  Site-B host (chapter 5)
#   NATS client          4222      14222        24222
#   NATS HTTP monitoring 8222      18222        28222
#   NATS gateway         7222      (network)    (network)
#   NATS cluster         6222      (network)    (network)
#   Mongo                27017     27117        27217
#   Cassandra            9042      19042        29042
#   Elasticsearch        9200      19200        29200
#   Valkey               6379      16379        26379
#   Keycloak             8080      18180        28180
#   auth-service         8080      18080        28080
#
# `make deps-up` keeps stock ports (4222, 27017, 9042, 9200, 6379, 8180); the
# e2e Makefile target refuses to start if `deps-up` is running.
#
# Trust model deviation from production:
#   The e2e supercluster runs with ONE shared operator + ONE shared chatapp
#   account across both sites. auth-service-a and auth-service-b both sign
#   user JWTs with the SAME chatapp seed (AUTH_SIGNING_KEY). NATS-level
#   subject permissions encode siteID so a forged subscribe on the wrong
#   site is denied, but the minted user JWT itself is bit-identical on both
#   sites -- a token issued by auth-service-a will be accepted by nats-b
#   directly. This is acceptable for the test environment but is NOT how
#   production trust isolation works. Tests that need per-site token
#   isolation realism must mock that boundary at a higher layer or wait for
#   a follow-up plan that introduces a per-site operator topology.


# Bound the default Docker json-file driver so long-running containers don't
# fill /var/lib/docker over consecutive runs (ops-reviewer finding).
x-logging: &default-logging
  driver: json-file
  options:
    max-size: "10m"
    max-file: "3"

networks:
  chat-e2e:
    driver: bridge

volumes:
  # site A (chapter 3)
  mongo-a-data:
  cass-a-data:
  es-a-data:
  keycloak-a-data:
  nats-a-data:
  # site B (chapter 5)
  mongo-b-data:
  cass-b-data:
  es-b-data:
  keycloak-b-data:
  nats-b-data:

services:
  # --- Site A dependencies (chapter 3) -----------------------------------

  nats-a:
    image: ${E2E_NATS_IMAGE:-nats:2.11-alpine}
    container_name: nats-a
    logging: *default-logging
    networks: [chat-e2e]
    ports:
      - "14222:4222"   # client (host-exposed for harness + manual debugging)
      - "18222:8222"   # http monitoring
    volumes:
      - ./nats/nats-a.conf:/etc/nats/nats-server.conf:ro
      - ./secrets/auth.conf:/etc/nats/auth.conf:ro
      - nats-a-data:/data/jetstream
    command: ["-c", "/etc/nats/nats-server.conf"]
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O- http://localhost:8222/healthz | grep -q ok"]
      interval: 2s
      timeout: 2s
      retries: 30
      start_period: 5s

  mongo-a:
    image: ${E2E_MONGO_IMAGE:-mongo:8}
    container_name: mongo-a
    logging: *default-logging
    networks: [chat-e2e]
    ports:
      - "27117:27017"
    volumes:
      - mongo-a-data:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--quiet", "--eval", "db.adminCommand('ping').ok"]
      interval: 2s
      timeout: 5s
      retries: 30
      start_period: 5s

  cass-a:
    image: ${E2E_CASS_IMAGE:-cassandra:5}
    container_name: cass-a
    logging: *default-logging
    networks: [chat-e2e]
    ports:
      - "19042:9042"
    volumes:
      - cass-a-data:/var/lib/cassandra
    environment:
      CASSANDRA_CLUSTER_NAME: chat-e2e-a
      CASSANDRA_DC: dc-a
      CASSANDRA_ENDPOINT_SNITCH: GossipingPropertyFileSnitch
      # Per amendment R1 3.C: cap the heap so two-site Cassandra fits on a
      # 32GB box alongside Mongo, ES, Keycloak. Cassandra:5 otherwise sizes
      # the heap to ~1/4 of host RAM (≈8GB) which OOMs the stack.
      JVM_OPTS: "-Xms512m -Xmx1g"
      MAX_HEAP_SIZE: "1G"
      HEAP_NEWSIZE: "256M"
    healthcheck:
      test: ["CMD-SHELL", "cqlsh -e 'describe keyspaces' >/dev/null 2>&1"]
      interval: 5s
      timeout: 5s
      retries: 60
      start_period: 30s

  # One-shot init job: applies the keyspace + table DDL once cass-a is
  # healthy. Per amendment R1 3.A: cassandra:5 has no docker-entrypoint
  # init hook, so a separate job that shells out to `cqlsh` is the working
  # pattern (mirrors docker-local/compose.deps.yaml's cassandra-init job).
  #
  # NOT profile-gated (revised per chapter 4 self-review): worker services
  # depend on this via `service_completed_successfully` so they don't start
  # against a Cassandra missing the `chat` keyspace. The CQL files use
  # CREATE ... IF NOT EXISTS so this is idempotent across restarts.
  cassandra-init-a:
    image: ${E2E_CASS_IMAGE:-cassandra:5}
    container_name: cassandra-init-a
    logging: *default-logging
    networks: [chat-e2e]
    depends_on:
      cass-a: { condition: service_healthy }
    volumes:
      - ../cassandra/init:/init:ro
    restart: "no"
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        set -eu
        for f in $$(ls /init/*.cql | sort); do
          echo "[cassandra-init-a] applying $$f"
          cqlsh cass-a -f "$$f"
        done
        echo "[cassandra-init-a] done"

  es-a:
    image: ${E2E_ES_IMAGE:-elasticsearch:8.17.0}
    container_name: es-a
    logging: *default-logging
    networks: [chat-e2e]
    ports:
      - "19200:9200"
    volumes:
      - es-a-data:/usr/share/elasticsearch/data
    environment:
      discovery.type: single-node
      xpack.security.enabled: "false"
      ES_JAVA_OPTS: "-Xms512m -Xmx512m"
    healthcheck:
      test: ["CMD-SHELL", "curl -fs http://localhost:9200/_cluster/health | grep -qE '\"status\":\"(green|yellow)\"'"]
      interval: 3s
      timeout: 3s
      retries: 30
      start_period: 15s

  # One-shot init job: applies an index template to ES so messages indices
  # have a 200ms refresh_interval (vs the 30s default). This makes search-
  # after-write deterministic for the e2e suite. Per amendment R1 4.B.
  # NOT profile-gated (revised per chapter 4 self-review) for the same
  # reason as cassandra-init-a: search-service-a + search-sync-worker-a
  # depend on this completing.
  search-init-a:
    image: curlimages/curl:8.20.0
    container_name: search-init-a
    logging: *default-logging
    networks: [chat-e2e]
    depends_on:
      es-a: { condition: service_healthy }
    restart: "no"
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        set -e
        # Retry both PUTs because ES can be `yellow` (healthcheck-passing) for
        # a few seconds before the master is ready to accept cluster-settings
        # writes (ops reviewer finding). 15 * 2s = 30s budget.
        for i in $$(seq 1 15); do
          curl -fsS -X PUT http://es-a:9200/_cluster/settings \
            -H 'Content-Type: application/json' \
            -d '{"persistent":{"cluster.routing.allocation.disk.threshold_enabled":false}}' \
            && break
          sleep 2
        done
        curl -fsS -X PUT http://es-a:9200/_index_template/messages-e2e \
          -H 'Content-Type: application/json' \
          -d '{
            "index_patterns": ["messages-sitea-v1-*", "messages-siteb-v1-*"],
            "template": { "settings": { "index": { "refresh_interval": "200ms", "number_of_replicas": 0 } } },
            "priority": 200
          }'
        echo "[search-init-a] template applied"

  valkey-a:
    image: ${E2E_VALKEY_IMAGE:-valkey/valkey:8}
    container_name: valkey-a
    logging: *default-logging
    networks: [chat-e2e]
    ports:
      - "16379:6379"
    healthcheck:
      test: ["CMD", "valkey-cli", "ping"]
      interval: 2s
      timeout: 2s
      retries: 30
      start_period: 2s

  keycloak-a:
    image: ${E2E_KEYCLOAK_IMAGE:-quay.io/keycloak/keycloak:26.0}
    container_name: keycloak-a
    logging: *default-logging
    networks: [chat-e2e]
    ports:
      - "18180:8080"
    environment:
      KC_BOOTSTRAP_ADMIN_USERNAME: admin
      KC_BOOTSTRAP_ADMIN_PASSWORD: admin
      KC_HEALTH_ENABLED: "true"
      KC_HTTP_ENABLED: "true"
      # Per amendment R1 3.D: align KC_HOSTNAME with the host-exposed URL so
      # tokens minted by Keycloak carry an `iss` claim that matches what
      # auth-service-a (in the docker network) verifies. host.docker.internal
      # resolves from inside the container via extra_hosts:host-gateway
      # (added on auth-service-a in chapter 4); on the host it resolves
      # natively on Docker Desktop, or via a one-line /etc/hosts entry on
      # plain Linux Docker Engine. The setup-e2e.sh script (chapter 4) warns
      # the user if resolution is missing.
      KC_HOSTNAME: "http://host.docker.internal:18180"
      KC_HOSTNAME_STRICT: "false"
      KC_HOSTNAME_BACKCHANNEL_DYNAMIC: "false"
    volumes:
      - ../../auth-service/deploy/keycloak/realm-export.json:/opt/keycloak/data/import/realm-export.json:ro
      - keycloak-a-data:/opt/keycloak/data
    command: ["start-dev", "--import-realm"]
    healthcheck:
      # Per amendment R1 3.B: the official Keycloak 26 image runs `sh`, not
      # bash, so the /dev/tcp exec idiom doesn't work. curl ships in the
      # image and reads the management-port /health/ready endpoint cleanly.
      test: ["CMD-SHELL", "bash -c 'exec 3<>/dev/tcp/localhost/9000 && printf \"GET /health/ready HTTP/1.0\\r\\n\\r\\n\" >&3 && grep -q UP <&3'"]
      interval: 5s
      timeout: 5s
      retries: 60
      start_period: 30s

  # --- Site A services (chapter 4) ---------------------------------------
  #
  # Common env vars applied to every service block below (and to site B in
  # chapter 5):
  #   - NATS_CREDS_FILE=/etc/nats/backend.creds (mounted via volumes)
  #   - OTEL_SDK_DISABLED=true (R1 4.B + R3.B; pkg/otelutil honors it as of
  #     commit 93ed27f, so workers don't spam OTLP-connect errors during e2e)
  # Workers do not define docker healthchecks (per amendment R4.C); the
  # harness instead polls for each worker's JetStream durable to exist via
  # awaitDurableReady (lands in chapter 7).

  auth-service-a:
    build: { context: ../.., dockerfile: auth-service/deploy/Dockerfile }
    container_name: auth-service-a
    logging: *default-logging
    networks: [chat-e2e]
    depends_on:
      nats-a:     { condition: service_healthy }
      keycloak-a: { condition: service_healthy }
    env_file:
      - ./secrets/e2e-secrets.env
    environment:
      PORT: "8080"
      DEV_MODE: "false"
      NATS_JWT_EXPIRY: "2h"
      # Per amendments R1 3.D + R2.A: iss claim alignment via
      # host.docker.internal:18180. extra_hosts maps that name to the docker
      # host gateway so auth-service-a can reach keycloak-a via the host
      # port-publish (same URL the harness uses, same `iss` in tokens).
      OIDC_ISSUER_URL: http://host.docker.internal:18180/realms/chatapp
      OIDC_AUDIENCES: nats-chat
      TLS_SKIP_VERIFY: "false"
      SITE_ID: siteA
      OTEL_SDK_DISABLED: "true"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "18080:8080"   # harness POSTs to this (R2.A: site-A on +10000 band)

  room-service-a:
    build: { context: ../.., dockerfile: room-service/deploy/Dockerfile }
    container_name: room-service-a
    logging: *default-logging
    networks: [chat-e2e]
    depends_on:
      nats-a:    { condition: service_healthy }
      mongo-a:   { condition: service_healthy }
      valkey-a:  { condition: service_healthy }
    environment:
      NATS_URL: nats://nats-a:4222
      NATS_CREDS_FILE: /etc/nats/backend.creds
      SITE_ID: siteA
      MONGO_URI: mongodb://mongo-a:27017
      MONGO_DB: chat
      MAX_ROOM_SIZE: "1000"
      MAX_BATCH_SIZE: "500"
      VALKEY_ADDR: valkey-a:6379
      VALKEY_KEY_GRACE_PERIOD: 24h
      BOOTSTRAP_STREAMS: "true"
      OTEL_SDK_DISABLED: "true"
    volumes:
      - ./secrets/backend.creds:/etc/nats/backend.creds:ro

  message-gatekeeper-a:
    build: { context: ../.., dockerfile: message-gatekeeper/deploy/Dockerfile }
    container_name: message-gatekeeper-a
    logging: *default-logging
    networks: [chat-e2e]
    depends_on:
      nats-a:  { condition: service_healthy }
      mongo-a: { condition: service_healthy }
    environment:
      NATS_URL: nats://nats-a:4222
      NATS_CREDS_FILE: /etc/nats/backend.creds
      SITE_ID: siteA
      MONGO_URI: mongodb://mongo-a:27017
      MONGO_DB: chat
      CHAT_BASE_URL: http://localhost:3000
      BOOTSTRAP_STREAMS: "true"
      OTEL_SDK_DISABLED: "true"
    volumes:
      - ./secrets/backend.creds:/etc/nats/backend.creds:ro

  message-worker-a:
    build: { context: ../.., dockerfile: message-worker/deploy/Dockerfile }
    container_name: message-worker-a
    logging: *default-logging
    networks: [chat-e2e]
    depends_on:
      nats-a:           { condition: service_healthy }
      mongo-a:          { condition: service_healthy }
      cass-a:           { condition: service_healthy }
      cassandra-init-a: { condition: service_completed_successfully }
    environment:
      NATS_URL: nats://nats-a:4222
      NATS_CREDS_FILE: /etc/nats/backend.creds
      SITE_ID: siteA
      CASSANDRA_HOSTS: cass-a
      CASSANDRA_KEYSPACE: chat
      MONGO_URI: mongodb://mongo-a:27017
      MONGO_DB: chat
      BOOTSTRAP_STREAMS: "true"
      OTEL_SDK_DISABLED: "true"
    volumes:
      - ./secrets/backend.creds:/etc/nats/backend.creds:ro

  broadcast-worker-a:
    build: { context: ../.., dockerfile: broadcast-worker/deploy/Dockerfile }
    container_name: broadcast-worker-a
    logging: *default-logging
    networks: [chat-e2e]
    depends_on:
      nats-a:   { condition: service_healthy }
      mongo-a:  { condition: service_healthy }
      valkey-a: { condition: service_healthy }
    environment:
      NATS_URL: nats://nats-a:4222
      NATS_CREDS_FILE: /etc/nats/backend.creds
      SITE_ID: siteA
      MONGO_URI: mongodb://mongo-a:27017
      MONGO_DB: chat
      USER_CACHE_SIZE: "10000"
      USER_CACHE_TTL: "5m"
      VALKEY_ADDR: valkey-a:6379
      VALKEY_KEY_GRACE_PERIOD: 24h
      BOOTSTRAP_STREAMS: "true"
      ENCRYPTION_ENABLED: "false"
      OTEL_SDK_DISABLED: "true"
    volumes:
      - ./secrets/backend.creds:/etc/nats/backend.creds:ro

  notification-worker-a:
    build: { context: ../.., dockerfile: notification-worker/deploy/Dockerfile }
    container_name: notification-worker-a
    logging: *default-logging
    networks: [chat-e2e]
    depends_on:
      nats-a:  { condition: service_healthy }
      mongo-a: { condition: service_healthy }
    environment:
      NATS_URL: nats://nats-a:4222
      NATS_CREDS_FILE: /etc/nats/backend.creds
      SITE_ID: siteA
      MONGO_URI: mongodb://mongo-a:27017
      MONGO_DB: chat
      BOOTSTRAP_STREAMS: "true"
      OTEL_SDK_DISABLED: "true"
    volumes:
      - ./secrets/backend.creds:/etc/nats/backend.creds:ro

  history-service-a:
    build: { context: ../.., dockerfile: history-service/deploy/Dockerfile }
    container_name: history-service-a
    logging: *default-logging
    networks: [chat-e2e]
    depends_on:
      nats-a:           { condition: service_healthy }
      mongo-a:          { condition: service_healthy }
      cass-a:           { condition: service_healthy }
      cassandra-init-a: { condition: service_completed_successfully }
      valkey-a:         { condition: service_healthy }
    environment:
      # history-service uses prefixed nested config (per pkg internal/config).
      CASSANDRA_HOSTS: cass-a
      CASSANDRA_KEYSPACE: chat
      MONGO_URI: mongodb://mongo-a:27017
      MONGO_DB: chat
      NATS_URL: nats://nats-a:4222
      NATS_CREDS_FILE: /etc/nats/backend.creds
      SITE_ID: siteA
      VALKEY_ADDR: valkey-a:6379
      ENCRYPTION_ENABLED: "false"
      OTEL_SDK_DISABLED: "true"
    volumes:
      - ./secrets/backend.creds:/etc/nats/backend.creds:ro

  inbox-worker-a:
    build: { context: ../.., dockerfile: inbox-worker/deploy/Dockerfile }
    container_name: inbox-worker-a
    logging: *default-logging
    networks: [chat-e2e]
    depends_on:
      nats-a:  { condition: service_healthy }
      mongo-a: { condition: service_healthy }
    environment:
      NATS_URL: nats://nats-a:4222
      NATS_CREDS_FILE: /etc/nats/backend.creds
      SITE_ID: siteA
      MONGO_URI: mongodb://mongo-a:27017
      MONGO_DB: chat
      BOOTSTRAP_STREAMS: "true"
      OTEL_SDK_DISABLED: "true"
    volumes:
      - ./secrets/backend.creds:/etc/nats/backend.creds:ro

  room-worker-a:
    build: { context: ../.., dockerfile: room-worker/deploy/Dockerfile }
    container_name: room-worker-a
    logging: *default-logging
    networks: [chat-e2e]
    depends_on:
      nats-a:  { condition: service_healthy }
      mongo-a: { condition: service_healthy }
    environment:
      NATS_URL: nats://nats-a:4222
      NATS_CREDS_FILE: /etc/nats/backend.creds
      SITE_ID: siteA
      MONGO_URI: mongodb://mongo-a:27017
      MONGO_DB: chat
      BOOTSTRAP_STREAMS: "true"
      OTEL_SDK_DISABLED: "true"
    volumes:
      - ./secrets/backend.creds:/etc/nats/backend.creds:ro

  search-service-a:
    build: { context: ../.., dockerfile: search-service/deploy/Dockerfile }
    container_name: search-service-a
    logging: *default-logging
    networks: [chat-e2e]
    depends_on:
      nats-a:         { condition: service_healthy }
      es-a:           { condition: service_healthy }
      search-init-a:  { condition: service_completed_successfully }
      valkey-a:       { condition: service_healthy }
    environment:
      NATS_URL: nats://nats-a:4222
      NATS_CREDS_FILE: /etc/nats/backend.creds
      SITE_ID: siteA
      SEARCH_URL: http://es-a:9200
      SEARCH_BACKEND: elasticsearch
      # Per amendment R1 4.B + R3.C: explicit override; matches the
      # auto-derived default (user-room-siteA) for clarity.
      SEARCH_USER_ROOM_INDEX: user-room-sitea
      VALKEY_ADDR: valkey-a:6379
      VALKEY_PASSWORD: ""
      SEARCH_DOC_COUNTS: "25"
      SEARCH_MAX_DOC_COUNTS: "100"
      SEARCH_RESTRICTED_ROOMS_CACHE_TTL: 5m
      SEARCH_RECENT_WINDOW: 8760h
      SEARCH_REQUEST_TIMEOUT: 10s
      SEARCH_METRICS_ADDR: ":9090"
      OTEL_SDK_DISABLED: "true"
    volumes:
      - ./secrets/backend.creds:/etc/nats/backend.creds:ro

  search-sync-worker-a:
    build: { context: ../.., dockerfile: search-sync-worker/deploy/Dockerfile }
    container_name: search-sync-worker-a
    logging: *default-logging
    networks: [chat-e2e]
    depends_on:
      nats-a:        { condition: service_healthy }
      es-a:          { condition: service_healthy }
      search-init-a: { condition: service_completed_successfully }
    environment:
      NATS_URL: nats://nats-a:4222
      NATS_CREDS_FILE: /etc/nats/backend.creds
      SITE_ID: siteA
      SEARCH_URL: http://es-a:9200
      SEARCH_BACKEND: elasticsearch
      MSG_INDEX_PREFIX: messages-sitea-v1
      SPOTLIGHT_INDEX: spotlight-sitea-v1-chat
      USER_ROOM_INDEX: user-room-sitea
      BOOTSTRAP_STREAMS: "true"
      OTEL_SDK_DISABLED: "true"
    volumes:
      - ./secrets/backend.creds:/etc/nats/backend.creds:ro

  # --- Site B dependencies (chapter 5) -----------------------------------
  # Mirror of site A with port shifts per amendment R2.A (+20000 band) and
  # R4.B (Mongo on 27217 to stay below the Linux ephemeral range). Both
  # sites share the same auth.conf and backend.creds (generated once by
  # setup-e2e.sh) so JWTs federate over the gateway. dc-b distinguishes the
  # site-B Cassandra topology label.

  nats-b:
    image: ${E2E_NATS_IMAGE:-nats:2.11-alpine}
    container_name: nats-b
    logging: *default-logging
    networks: [chat-e2e]
    ports:
      - "24222:4222"
      - "28222:8222"
    volumes:
      - ./nats/nats-b.conf:/etc/nats/nats-server.conf:ro
      - ./secrets/auth.conf:/etc/nats/auth.conf:ro
      - nats-b-data:/data/jetstream
    command: ["-c", "/etc/nats/nats-server.conf"]
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O- http://localhost:8222/healthz | grep -q ok"]
      interval: 2s
      timeout: 2s
      retries: 30
      start_period: 5s

  mongo-b:
    image: ${E2E_MONGO_IMAGE:-mongo:8}
    container_name: mongo-b
    logging: *default-logging
    networks: [chat-e2e]
    ports:
      - "27217:27017"
    volumes:
      - mongo-b-data:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--quiet", "--eval", "db.adminCommand('ping').ok"]
      interval: 2s
      timeout: 5s
      retries: 30
      start_period: 5s

  cass-b:
    image: ${E2E_CASS_IMAGE:-cassandra:5}
    container_name: cass-b
    logging: *default-logging
    networks: [chat-e2e]
    ports:
      - "29042:9042"
    volumes:
      - cass-b-data:/var/lib/cassandra
    environment:
      CASSANDRA_CLUSTER_NAME: chat-e2e-b
      CASSANDRA_DC: dc-b
      CASSANDRA_ENDPOINT_SNITCH: GossipingPropertyFileSnitch
      JVM_OPTS: "-Xms512m -Xmx1g"
      MAX_HEAP_SIZE: "1G"
      HEAP_NEWSIZE: "256M"
    healthcheck:
      test: ["CMD-SHELL", "cqlsh -e 'describe keyspaces' >/dev/null 2>&1"]
      interval: 5s
      timeout: 5s
      retries: 60
      start_period: 30s

  cassandra-init-b:
    image: ${E2E_CASS_IMAGE:-cassandra:5}
    container_name: cassandra-init-b
    logging: *default-logging
    networks: [chat-e2e]
    depends_on:
      cass-b: { condition: service_healthy }
    volumes:
      - ../cassandra/init:/init:ro
    restart: "no"
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        set -eu
        for f in $$(ls /init/*.cql | sort); do
          echo "[cassandra-init-b] applying $$f"
          cqlsh cass-b -f "$$f"
        done
        echo "[cassandra-init-b] done"

  es-b:
    image: ${E2E_ES_IMAGE:-elasticsearch:8.17.0}
    container_name: es-b
    logging: *default-logging
    networks: [chat-e2e]
    ports:
      - "29200:9200"
    volumes:
      - es-b-data:/usr/share/elasticsearch/data
    environment:
      discovery.type: single-node
      xpack.security.enabled: "false"
      ES_JAVA_OPTS: "-Xms512m -Xmx512m"
    healthcheck:
      test: ["CMD-SHELL", "curl -fs http://localhost:9200/_cluster/health | grep -qE '\"status\":\"(green|yellow)\"'"]
      interval: 3s
      timeout: 3s
      retries: 30
      start_period: 15s

  # search-init-a (chapter 3) already applies the messages-* index template
  # across both site-A and site-B index patterns (priority 200), so a
  # site-B-specific search-init isn't strictly needed for the messages
  # indices. But: search-init-a targets es-a, NOT es-b -- ES instances are
  # independent in this topology (one ES per site, no replication). So
  # site-B's ES needs its own template-application job.
  search-init-b:
    image: curlimages/curl:8.20.0
    container_name: search-init-b
    logging: *default-logging
    networks: [chat-e2e]
    depends_on:
      es-b: { condition: service_healthy }
    restart: "no"
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        set -e
        # Retry both PUTs because ES can be `yellow` (healthcheck-passing) for
        # a few seconds before the master is ready to accept cluster-settings
        # writes (ops reviewer finding). 15 * 2s = 30s budget.
        for i in $$(seq 1 15); do
          curl -fsS -X PUT http://es-b:9200/_cluster/settings \
            -H 'Content-Type: application/json' \
            -d '{"persistent":{"cluster.routing.allocation.disk.threshold_enabled":false}}' \
            && break
          sleep 2
        done
        curl -fsS -X PUT http://es-b:9200/_index_template/messages-e2e \
          -H 'Content-Type: application/json' \
          -d '{
            "index_patterns": ["messages-sitea-v1-*", "messages-siteb-v1-*"],
            "template": { "settings": { "index": { "refresh_interval": "200ms", "number_of_replicas": 0 } } },
            "priority": 200
          }'
        echo "[search-init-b] template applied"

  valkey-b:
    image: ${E2E_VALKEY_IMAGE:-valkey/valkey:8}
    container_name: valkey-b
    logging: *default-logging
    networks: [chat-e2e]
    ports:
      - "26379:6379"
    healthcheck:
      test: ["CMD", "valkey-cli", "ping"]
      interval: 2s
      timeout: 2s
      retries: 30
      start_period: 2s

  keycloak-b:
    image: ${E2E_KEYCLOAK_IMAGE:-quay.io/keycloak/keycloak:26.0}
    container_name: keycloak-b
    logging: *default-logging
    networks: [chat-e2e]
    ports:
      - "28180:8080"
    environment:
      KC_BOOTSTRAP_ADMIN_USERNAME: admin
      KC_BOOTSTRAP_ADMIN_PASSWORD: admin
      KC_HEALTH_ENABLED: "true"
      KC_HTTP_ENABLED: "true"
      KC_HOSTNAME: "http://host.docker.internal:28180"
      KC_HOSTNAME_STRICT: "false"
      KC_HOSTNAME_BACKCHANNEL_DYNAMIC: "false"
    volumes:
      - ../../auth-service/deploy/keycloak/realm-export.json:/opt/keycloak/data/import/realm-export.json:ro
      - keycloak-b-data:/opt/keycloak/data
    command: ["start-dev", "--import-realm"]
    healthcheck:
      test: ["CMD-SHELL", "bash -c 'exec 3<>/dev/tcp/localhost/9000 && printf \"GET /health/ready HTTP/1.0\\r\\n\\r\\n\" >&3 && grep -q UP <&3'"]
      interval: 5s
      timeout: 5s
      retries: 60
      start_period: 30s

  # --- Site B services (chapter 5) ---------------------------------------
  # Mechanical mirror of site-A blocks per amendment R1 4.B (OTEL_SDK_
  # DISABLED on all), R2.A (auth-service-b on 28080:8080), and the trust-
  # model deviation noted at the top of this file (shared chatapp account).

  auth-service-b:
    build: { context: ../.., dockerfile: auth-service/deploy/Dockerfile }
    container_name: auth-service-b
    logging: *default-logging
    networks: [chat-e2e]
    depends_on:
      nats-b:     { condition: service_healthy }
      keycloak-b: { condition: service_healthy }
    env_file:
      - ./secrets/e2e-secrets.env
    environment:
      PORT: "8080"
      DEV_MODE: "false"
      NATS_JWT_EXPIRY: "2h"
      OIDC_ISSUER_URL: http://host.docker.internal:28180/realms/chatapp
      OIDC_AUDIENCES: nats-chat
      TLS_SKIP_VERIFY: "false"
      SITE_ID: siteB
      OTEL_SDK_DISABLED: "true"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "28080:8080"

  room-service-b:
    build: { context: ../.., dockerfile: room-service/deploy/Dockerfile }
    container_name: room-service-b
    logging: *default-logging
    networks: [chat-e2e]
    depends_on:
      nats-b:    { condition: service_healthy }
      mongo-b:   { condition: service_healthy }
      valkey-b:  { condition: service_healthy }
    environment:
      NATS_URL: nats://nats-b:4222
      NATS_CREDS_FILE: /etc/nats/backend.creds
      SITE_ID: siteB
      MONGO_URI: mongodb://mongo-b:27017
      MONGO_DB: chat
      MAX_ROOM_SIZE: "1000"
      MAX_BATCH_SIZE: "500"
      VALKEY_ADDR: valkey-b:6379
      VALKEY_KEY_GRACE_PERIOD: 24h
      BOOTSTRAP_STREAMS: "true"
      OTEL_SDK_DISABLED: "true"
    volumes:
      - ./secrets/backend.creds:/etc/nats/backend.creds:ro

  message-gatekeeper-b:
    build: { context: ../.., dockerfile: message-gatekeeper/deploy/Dockerfile }
    container_name: message-gatekeeper-b
    logging: *default-logging
    networks: [chat-e2e]
    depends_on:
      nats-b:  { condition: service_healthy }
      mongo-b: { condition: service_healthy }
    environment:
      NATS_URL: nats://nats-b:4222
      NATS_CREDS_FILE: /etc/nats/backend.creds
      SITE_ID: siteB
      MONGO_URI: mongodb://mongo-b:27017
      MONGO_DB: chat
      CHAT_BASE_URL: http://localhost:3000
      BOOTSTRAP_STREAMS: "true"
      OTEL_SDK_DISABLED: "true"
    volumes:
      - ./secrets/backend.creds:/etc/nats/backend.creds:ro

  message-worker-b:
    build: { context: ../.., dockerfile: message-worker/deploy/Dockerfile }
    container_name: message-worker-b
    logging: *default-logging
    networks: [chat-e2e]
    depends_on:
      nats-b:           { condition: service_healthy }
      mongo-b:          { condition: service_healthy }
      cass-b:           { condition: service_healthy }
      cassandra-init-b: { condition: service_completed_successfully }
    environment:
      NATS_URL: nats://nats-b:4222
      NATS_CREDS_FILE: /etc/nats/backend.creds
      SITE_ID: siteB
      CASSANDRA_HOSTS: cass-b
      CASSANDRA_KEYSPACE: chat
      MONGO_URI: mongodb://mongo-b:27017
      MONGO_DB: chat
      BOOTSTRAP_STREAMS: "true"
      OTEL_SDK_DISABLED: "true"
    volumes:
      - ./secrets/backend.creds:/etc/nats/backend.creds:ro

  broadcast-worker-b:
    build: { context: ../.., dockerfile: broadcast-worker/deploy/Dockerfile }
    container_name: broadcast-worker-b
    logging: *default-logging
    networks: [chat-e2e]
    depends_on:
      nats-b:   { condition: service_healthy }
      mongo-b:  { condition: service_healthy }
      valkey-b: { condition: service_healthy }
    environment:
      NATS_URL: nats://nats-b:4222
      NATS_CREDS_FILE: /etc/nats/backend.creds
      SITE_ID: siteB
      MONGO_URI: mongodb://mongo-b:27017
      MONGO_DB: chat
      USER_CACHE_SIZE: "10000"
      USER_CACHE_TTL: "5m"
      VALKEY_ADDR: valkey-b:6379
      VALKEY_KEY_GRACE_PERIOD: 24h
      BOOTSTRAP_STREAMS: "true"
      ENCRYPTION_ENABLED: "false"
      OTEL_SDK_DISABLED: "true"
    volumes:
      - ./secrets/backend.creds:/etc/nats/backend.creds:ro

  notification-worker-b:
    build: { context: ../.., dockerfile: notification-worker/deploy/Dockerfile }
    container_name: notification-worker-b
    logging: *default-logging
    networks: [chat-e2e]
    depends_on:
      nats-b:  { condition: service_healthy }
      mongo-b: { condition: service_healthy }
    environment:
      NATS_URL: nats://nats-b:4222
      NATS_CREDS_FILE: /etc/nats/backend.creds
      SITE_ID: siteB
      MONGO_URI: mongodb://mongo-b:27017
      MONGO_DB: chat
      BOOTSTRAP_STREAMS: "true"
      OTEL_SDK_DISABLED: "true"
    volumes:
      - ./secrets/backend.creds:/etc/nats/backend.creds:ro

  history-service-b:
    build: { context: ../.., dockerfile: history-service/deploy/Dockerfile }
    container_name: history-service-b
    logging: *default-logging
    networks: [chat-e2e]
    depends_on:
      nats-b:           { condition: service_healthy }
      mongo-b:          { condition: service_healthy }
      cass-b:           { condition: service_healthy }
      cassandra-init-b: { condition: service_completed_successfully }
      valkey-b:         { condition: service_healthy }
    environment:
      CASSANDRA_HOSTS: cass-b
      CASSANDRA_KEYSPACE: chat
      MONGO_URI: mongodb://mongo-b:27017
      MONGO_DB: chat
      NATS_URL: nats://nats-b:4222
      NATS_CREDS_FILE: /etc/nats/backend.creds
      SITE_ID: siteB
      VALKEY_ADDR: valkey-b:6379
      ENCRYPTION_ENABLED: "false"
      OTEL_SDK_DISABLED: "true"
    volumes:
      - ./secrets/backend.creds:/etc/nats/backend.creds:ro

  inbox-worker-b:
    build: { context: ../.., dockerfile: inbox-worker/deploy/Dockerfile }
    container_name: inbox-worker-b
    logging: *default-logging
    networks: [chat-e2e]
    depends_on:
      nats-b:  { condition: service_healthy }
      mongo-b: { condition: service_healthy }
    environment:
      NATS_URL: nats://nats-b:4222
      NATS_CREDS_FILE: /etc/nats/backend.creds
      SITE_ID: siteB
      MONGO_URI: mongodb://mongo-b:27017
      MONGO_DB: chat
      BOOTSTRAP_STREAMS: "true"
      OTEL_SDK_DISABLED: "true"
    volumes:
      - ./secrets/backend.creds:/etc/nats/backend.creds:ro

  room-worker-b:
    build: { context: ../.., dockerfile: room-worker/deploy/Dockerfile }
    container_name: room-worker-b
    logging: *default-logging
    networks: [chat-e2e]
    depends_on:
      nats-b:  { condition: service_healthy }
      mongo-b: { condition: service_healthy }
    environment:
      NATS_URL: nats://nats-b:4222
      NATS_CREDS_FILE: /etc/nats/backend.creds
      SITE_ID: siteB
      MONGO_URI: mongodb://mongo-b:27017
      MONGO_DB: chat
      BOOTSTRAP_STREAMS: "true"
      OTEL_SDK_DISABLED: "true"
    volumes:
      - ./secrets/backend.creds:/etc/nats/backend.creds:ro

  search-service-b:
    build: { context: ../.., dockerfile: search-service/deploy/Dockerfile }
    container_name: search-service-b
    logging: *default-logging
    networks: [chat-e2e]
    depends_on:
      nats-b:        { condition: service_healthy }
      es-b:          { condition: service_healthy }
      search-init-b: { condition: service_completed_successfully }
      valkey-b:      { condition: service_healthy }
    environment:
      NATS_URL: nats://nats-b:4222
      NATS_CREDS_FILE: /etc/nats/backend.creds
      SITE_ID: siteB
      SEARCH_URL: http://es-b:9200
      SEARCH_BACKEND: elasticsearch
      SEARCH_USER_ROOM_INDEX: user-room-siteb
      VALKEY_ADDR: valkey-b:6379
      VALKEY_PASSWORD: ""
      SEARCH_DOC_COUNTS: "25"
      SEARCH_MAX_DOC_COUNTS: "100"
      SEARCH_RESTRICTED_ROOMS_CACHE_TTL: 5m
      SEARCH_RECENT_WINDOW: 8760h
      SEARCH_REQUEST_TIMEOUT: 10s
      SEARCH_METRICS_ADDR: ":9090"
      OTEL_SDK_DISABLED: "true"
    # No `ports:` block: site-B metrics are not host-exposed (avoids 9090
    # collision with site-A's search-service-a). Reach via docker network
    # from the harness if a future test needs it.
    volumes:
      - ./secrets/backend.creds:/etc/nats/backend.creds:ro

  search-sync-worker-b:
    build: { context: ../.., dockerfile: search-sync-worker/deploy/Dockerfile }
    container_name: search-sync-worker-b
    logging: *default-logging
    networks: [chat-e2e]
    depends_on:
      nats-b:        { condition: service_healthy }
      es-b:          { condition: service_healthy }
      search-init-b: { condition: service_completed_successfully }
    environment:
      NATS_URL: nats://nats-b:4222
      NATS_CREDS_FILE: /etc/nats/backend.creds
      SITE_ID: siteB
      SEARCH_URL: http://es-b:9200
      SEARCH_BACKEND: elasticsearch
      MSG_INDEX_PREFIX: messages-siteb-v1
      SPOTLIGHT_INDEX: spotlight-siteb-v1-chat
      USER_ROOM_INDEX: user-room-siteb
      BOOTSTRAP_STREAMS: "true"
      OTEL_SDK_DISABLED: "true"
    volumes:
      - ./secrets/backend.creds:/etc/nats/backend.creds:ro

server_name: nats-site-a
# Leafnode hub. site-b dials in here as a leaf using backend.creds (same
# chatapp account as site-a). The leaf link is the cross-site transport.
# See docs/integration-suite-multisite-findings.md F-001 for the trail.
leafnodes: {
  port: 7422
}
jetstream: {
  domain: site-a
  store_dir: /data/jetstream
}
